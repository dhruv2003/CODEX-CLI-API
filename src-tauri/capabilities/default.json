{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Only the bundled local launcher receives native permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
